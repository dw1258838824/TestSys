<!DOCTYPE html>
<html lang="zh" xmlns:th="http://www.thymeleaf.org" xmlns:shiro="http://www.pollix.at/thymeleaf/shiro">
<head>
    <th:block th:include="include :: header('报名管理列表')" />
</head>
<body class="gray-bg">
     <div class="container-div">
        <div class="row">
            <div class="col-sm-12 search-collapse">
                <form id="formId">
                    <div class="select-list">
                        <ul>
                            <li>
                                <label>准考状态：</label>
                                <select name="state" th:with="type=${@dict.getType('sys_register_state')}">
                                    <option value="">所有</option>
                                    <option th:each="dict : ${type}" th:text="${dict.dictLabel}" th:value="${dict.dictValue}"></option>
                                </select>
                            </li>
                            <li>
                                <label>准考证号：</label>
                                <input type="text" name="examCode"/>
                            </li>
                            <li class="select-time">
                                <label>报名时间：</label>
                                <input type="text" class="time-input" id="startTime" placeholder="开始时间" name="params[beginCtime]"/>
                                <span>-</span>
                                <input type="text" class="time-input" id="endTime" placeholder="结束时间" name="params[endCtime]"/>
                            </li>
                            <li>
                                <a class="btn btn-primary btn-rounded btn-sm" onclick="$.table.search()"><i class="fa fa-search"></i>&nbsp;搜索</a>
                                <a class="btn btn-warning btn-rounded btn-sm" onclick="$.form.reset()"><i class="fa fa-refresh"></i>&nbsp;重置</a>
                            </li>
                        </ul>
                    </div>
                </form>
            </div>
            <div class="col-sm-12 select-table table-striped">
                <table id="bootstrap-table"></table>
            </div>
        </div>
    </div>
    <th:block th:include="include :: footer" />
    <script th:inline="javascript">
        var stateDatas = [[${@dict.getType('sys_register_state')}]];
        var prefix = ctx + "student/register";

        $(function() {
            var options = {
                url: prefix + "/list",
                modalName: "报名管理",
                columns: [
                {
                    field: 'params.examTitle', 
                    title: '试卷名称'
                },
                {
                    field: 'examCode', 
                    title: '准考证号'
                },
                {
                    field: 'state', 
                    title: '准考状态',
                    formatter: function(value, row, index) {
                       return $.table.selectDictLabel(stateDatas, value);
                    }
                },
                {
                    field: 'params.beginTime', 
                    title: '考试开始时间'
                },
                {
                    field: 'params.endTime', 
                    title: '考试结束时间'
                },
                {
                    field: 'ctime', 
                    title: '报名时间'
                },
                {
                    title: '操作',
                    align: 'center',
                    formatter: function(value, row, index) {
                        var actions = [];
                        actions.push('<button class="btn btn-success btn-xs" onclick="start(\'' + row.examId + '\')"><i class="fa fa-edit"></i>开始考试</button> ');
                        return actions.join('');
                    }
                }]
            };
            $.table.init(options);
            
        });
        
        function start(examId){
        	window.open("exam/start/" + examId);
        }
    </script>
</body>
</html>