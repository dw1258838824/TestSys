<!DOCTYPE html>
<html lang="zh" xmlns:th="http://www.thymeleaf.org" >
<head>
    <th:block th:include="include :: header('修改题库')" />
</head>
<body class="white-bg">
    <div class="wrapper wrapper-content animated fadeInRight ibox-content">
        <form class="form-horizontal m" id="form-question-edit" th:object="${tQuestion}">
            <input name="questionId" th:field="*{questionId}" type="hidden">
            <div class="form-group">    
                <label class="col-sm-3 control-label">题目类型：</label>
                <div class="col-sm-8">
                	<input id="questionTypeIdv" type="hidden"  th:field="*{questionTypeId}" />
                    <select name="questionTypeId" class="form-control m-b" required>
                    </select>
                </div>
            </div>
            <div id="questionDiv" class="questionDiv">
	            <div class="form-group">    
	                <label class="col-sm-3 control-label is-required">题目标题：</label>
	                <div class="col-sm-8">
	                    <textarea name="questionTitle" class="form-control" required>[[*{questionTitle}]]</textarea>
	                </div>
	            </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label is-required">题目内容：</label>
                <div class="col-sm-8">
                    <input name="questionHtml" th:field="*{questionHtml}" class="form-control" type="text" required>
                </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label">题目答案：</label>
                <div class="col-sm-8">
                    <input name="questionAnswer" th:field="*{questionAnswer}" class="form-control" type="text">
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">答案解析：</label>
                <div class="col-sm-8">
                    <textarea name="questionAnswerAnalysis" class="form-control">[[*{questionAnswerAnalysis}]]</textarea>
                </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label">等级：</label>
                <div class="col-sm-8">
                	<input id="levelIdv" type="hidden"  th:field="*{levelId}" />
                    <select name="levelId" class="form-control m-b" required>
                    </select>
                </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label">科目：</label>
                <div class="col-sm-8">
                	<input id="subjectIdv" type="hidden"  th:field="*{subjectId}" />
                    <select name="subjectId" class="form-control m-b">
                    </select>
                </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label">知识点：</label>
                <div class="col-sm-8">
                	<input id="questionPointIdv" type="hidden"  th:field="*{questionPointId}" />
                    <select name="questionPointId" class="form-control m-b">
                    </select>
                </div>
            </div>
            <div class="form-group">    
                <label class="col-sm-3 control-label">是否删除：</label>
                <div class="col-sm-8">
                    <select name="isDelete" class="form-control m-b" th:with="type=${@dict.getType('sys_yes_no')}" required>
                        <option th:each="dict : ${type}" th:text="${dict.dictLabel}" th:value="${dict.dictValue}" th:field="*{isDelete}"></option>
                    </select>
                </div>
            </div>
        </form>
    </div>
    <th:block th:include="include :: footer" />
    <script type="text/javascript">
        var prefix = ctx + "system/question";
        $("#form-question-edit").validate({
            focusCleanup: true
        });
        function submitHandler() {
            if ($.validate.form()) {
            	getHtml();
                $.operate.save(prefix + "/edit", $('#form-question-edit').serialize());
            }
        }

        function getHtml(){//获取题目内容
        	var questionTitle = $("#questionTitle").val();
        	if(questionTitle.indexOf(">")>-1 || questionTitle.indexOf("<")>-1){
        		$.modal.msgError("标题或选项中请勿输入'<'、'>'字符 ");
        		return;
        	}
        		
        	var options = '<question><questionTitle>'+questionTitle+'</questionTitle>';
        	var idx = 0;
        	if($(".radioCode").length > 0){//单选题、判断题
            	options += "<questionRadio>";
            	$(".radioCode").each(function(){
            		var optionCode = $(this).val();
            		var optionCentext = $($(".radioCentext")[idx]).val();
            		options += "<options><code>"+optionCode+"</code><context>"+optionCentext+"</context></options>";
            		idx ++;
            	});
            	options += "</questionRadio>";
        	}else if($(".boxCode").length > 0){//多选题
        		options += "<questionCheckBox>";
            	$(".boxCode").each(function(){
            		var optionCode = $(this).val();
            		var optionCentext = $($(".boxCentext")[idx]).val();
            		options += "<options><code>"+optionCode+"</code><context>"+optionCentext+"</context></options>";
            		idx ++;
            	});
            	options += "</questionCheckBox>";
        	}else if($("#questionFile")){
        		options += "<questionFile>"+$("#questionFile").val()+"</questionFile>";
        	}
        	options += "</question>";
        	$("#questionHtml").val(options);
        }

        
        $(function(){
        	initTypeList("questionTypeId", $("#questionTypeIdv").val());
        	initLevelList("levelId",$("#levelIdv").val());
        	initSubjectList("subjectId",$("#subjectIdv").val());
        	initPointList("questionPointId",$("#questionPointIdv").val());
        	 $("select[name='questionTypeId']").change(function(){
             	$("#questionHtml").val("");
             	var index = parseInt($(this).find("option:selected").attr("lang"));
             	if(index>=0){
             		$("#questionDiv").html(typeHtmlList[index]);
             	}else{
             		$("#questionDiv").html("");
             	}
             });
             $(".radioCentext,#questionTitle").keyup(function(){
             	replaceHtml(this,"[<,>]");
             });
             $(".radioCentext,#questionTitle").blur(function(){
             	replaceHtml(this,"[<,>]");
             });
        })
        
    </script>
</body>
</html>